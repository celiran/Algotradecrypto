import { legacyPosts } from "../content/legacy-content";
import { getEffectiveArticle } from "../content/article-overrides";
const esc=(value:string)=>value.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
export async function GET(){const base="https://algotradecrypto.com";const items=legacyPosts.map(getEffectiveArticle).map(post=>`<item><title>${esc(post.title)}</title><link>${base}/${encodeURIComponent(post.slug)}/</link><guid>${base}/${encodeURIComponent(post.slug)}/</guid><pubDate>${new Date(post.modified??post.date??Date.now()).toUTCString()}</pubDate><description>${esc(post.excerpt)}</description></item>`).join("");const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>AlgoTradeCrypto — מרכז ידע</title><link>${base}/blog-2/</link><description>מאמרים על מסחר אוטומטי בקריפטו</description><language>he-IL</language>${items}</channel></rss>`;return new Response(xml,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, max-age=3600"}})}
